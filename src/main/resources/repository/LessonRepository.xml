<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdd.ela.repository.LessonRepository">
    <resultMap id="lesson" type="com.mdd.ela.dto.model.Lesson">
        <result column="id" property="id"/>
        <result column="chapter_id" property="chapterId"/>
        <result column="lesson_title" property="lessonTitle"/>
        <result column="lesson_url" property="lessonUrl"/>
        <result column="lesson_length" property="lessonLength"/>
        <result column="active" property="active"/>

        <result column="create_time" property="createTime"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="modify_time" property="modifyTime"/>
        <result column="modify_user_id" property="modifyUserId"/>
    </resultMap>


    <select id="findAll" resultMap="lesson">
        select * from lesson v
        where chapter_id = #{chapterId}
    </select>


    <select id="select" resultMap="lesson">
        select * from lesson v
        where v.id = #{id}
    </select>


    <insert id="insert">
        insert into lesson(
        chapter_id
        , lesson_title
        , lesson_url
        , lesson_length
        , active
        , create_time
        , create_user_id)
        values (
        #{chapterId},
        #{lessonTitle},
        #{lessonUrl},
        #{lessonLength},
        #{active},
        now(),
        #{createUserId})
    </insert>


    <update id="update">
        update lesson
        set
        lesson_title = #{lessonTitle}
        ,   lesson_url = #{lessonUrl}
        ,   lesson_length = #{lessonLength}
        ,   active = #{active}
        ,   modify_time = now()
        ,   modify_user_id = #{modifyUserId}
        where id = #{id}
    </update>


    <delete id="delete">
        delete from lesson
        where id = #{id}
    </delete>

</mapper>